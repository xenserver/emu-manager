emu-manager
===========

Debugging
---------

Run `make D=1` to build with optimizations turned off and debug logging turned
on. Alternatively pass `-debug true` to emu-manager to turn debug logging on
dynamically.
